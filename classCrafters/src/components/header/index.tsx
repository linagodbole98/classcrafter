// import { useGetPostsQuery } from 'src/services/index';

// function Posts() {
//   const { data, error, isLoading } = useGetPostsQuery();

//   if (isLoading) {
//     return <div>Loading...</div>;
//   }

//   if (error) {
//     return <div>Error: {error.message}</div>;
//   }

//   return (
//     <div>
//       {data.map((post) => (
//         <div key={post.id}>{post.title}</div>
//       ))}
//     </div>
//   );
// }

// export default Posts;
